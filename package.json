{
  "name": "tinyclaw",
  "version": "0.0.2",
  "description": "TinyClaw is a tiny agent that acts as your 24/7 personal assistant",
  "scripts": {
    "deploy": "cd ~/workspace/tinyclaw && GIT_MERGE_AUTOEDIT=no git pull && npm run restart",
    "restart": "npm run build && npm run features:install && ~/.local/bin/tinyclaw restart",
    "build": "tsc",
    "discord": "node dist/channels/discord-client.js",
    "queue": "node dist/queue-processor.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "husky",
    "features:install": "bash scripts/features.sh install",
    "features:uninstall": "bash scripts/features.sh uninstall"
  },
  "dependencies": {
    "discord.js": "^14.16.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "husky": "^9.1.7",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
